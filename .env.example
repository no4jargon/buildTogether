# Database
DATABASE_URL="file:./dev.db"
# For postgres, update DATABASE_URL, and use prisma/schema.postgres.prisma
# NEXTAUTH
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-secret"
# Email (optional for production; dev logs magic links)
EMAIL_FROM="no-reply@buildtogether.local"
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="smtp-user"
SMTP_PASS="smtp-password"
SMTP_SECURE="false"
